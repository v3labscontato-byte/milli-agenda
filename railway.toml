[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/api/Dockerfile"

[deploy]
preDeployCommand = "npx prisma migrate deploy --schema=packages/database/prisma/schema.prisma --skip-generate"
startCommand = "node dist/main"
restartPolicyType = "ON_FAILURE"
